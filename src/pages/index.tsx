import Kanban from '@/modules/Main/components';
import type { TPage } from '@/types';

const Index: TPage = () => {
  return (
    <div className="h-full w-full">
      <Kanban />
    </div>
  );
};

export default Index;
